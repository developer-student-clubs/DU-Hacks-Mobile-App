import 'package:flutter/material.dart';

class MarkPresence extends StatelessWidget {
  const MarkPresence({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}